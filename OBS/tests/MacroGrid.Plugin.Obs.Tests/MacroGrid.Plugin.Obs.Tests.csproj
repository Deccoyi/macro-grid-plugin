<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.4" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\MacroGrid.Plugin.Obs.csproj" />
    <!-- Tests need the SDK at runtime (plugin types are loaded), so no ExcludeAssets here. -->
    <PackageReference Include="MacroGrid.Plugin.Abstractions" Version="$(MacroGridSdkVersion)" Condition="'$(UseLocalSdk)' != 'true'" />
    <ProjectReference Include="$(LocalSdkProject)" Condition="'$(UseLocalSdk)' == 'true'" />
  </ItemGroup>

</Project>
